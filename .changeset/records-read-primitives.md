---
"@agent-crm/cli": patch
"@agent-crm/sdk": patch
---

Add cloud API-backed `acrm records list` and `acrm records show` read primitives so agents can resolve people, companies, and other records to `record_id`s before running write commands.
