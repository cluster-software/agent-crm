{
  "name": "@agent-crm/cli",
  "version": "0.0.1",
  "description": "Headless CRM for AI agents — versioned, queryable, scriptable",
  "type": "module",
  "bin": {
    "acrm": "./dist/bin/acrm.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/bin/acrm.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@lix-js/sdk": "0.6.0-preview.2",
    "better-sqlite3": "^12.9.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
